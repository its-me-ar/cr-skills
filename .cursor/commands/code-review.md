---
description: Detect repo stack, review user topics, write CODE-REVIEW.html
---

Follow `.cursor/skills/topic-code-review/SKILL.md`.

You will get a **repository**. **Figure out the tech stack yourself** from files and folders. Do not assume NestJS, Angular, or any framework.

**Topics** are whatever the user shares (use the list below if present; otherwise ask). Review only those topics.

$ARGUMENTS

Then:
1. Detect stack and entry points.
2. Static review of user topics only (optional browser if URL given; git only if that topic is selected).
3. Score 1–4. No Context lines.
4. Write `CODE-REVIEW.html` (h4 = 14px, body = 12px) from `.cursor/skills/topic-code-review/html-template.html`.
5. Chat: confirm the file path only.
